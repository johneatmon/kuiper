---
import { SITE } from "@data/config"
import BaseLayout from "@layouts/BaseLayout.astro"
import { smartypants } from "smartypants"
---

<BaseLayout title="Welcome to Kuiper" description="Wow!">
	<h1 set:html={smartypants(SITE.title, 1)} />
	<p set:html={smartypants(SITE.fallbackDesc, 1)} />
</BaseLayout>
